import Reveal from "../../Reveal/Reveal";
const Title = () => {
  return (
    <div className="flex flex-col gap-1 max-w-[33ch]">
      <Reveal className="w-full">
        <span className="lg:text-9xl  text-5xl font-semibold">
          TRI KIET IS RIGHT HERE !
        </span>
      </Reveal>
    </div>
  );
};

export default Title;
